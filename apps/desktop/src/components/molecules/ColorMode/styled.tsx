import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  top: 20px;
  right: 20px;
  text-align: center;
  cursor: pointer;

  & > p {
    margin: 0;
  }
`;
